% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{datum_dataset_add}
\alias{datum_dataset_add}
\title{Add a dataset to \code{.data.yaml}}
\usage{
datum_dataset_add(
  id,
  desc,
  target,
  source = NULL,
  sources = NULL,
  policy = NULL,
  config = ".data.yaml",
  executable = NULL,
  wd = NULL,
  timeout = getOption("datur.timeout", 300)
)
}
\arguments{
\item{id}{Unique dataset identifier.}

\item{desc}{Human-readable description.}

\item{target}{Local target path.}

\item{source}{One source created with \code{\link[=datum_source]{datum_source()}} or an equivalent named list.}

\item{sources}{A non-empty list of fallback sources. Supply exactly one of
\code{source} and \code{sources}.}

\item{policy}{Optional \code{"fail"}, \code{"update"}, or \code{"log"} override.}

\item{config}{Configuration path. A missing file is created with version 1.}

\item{executable}{Optional explicit executable path.}

\item{wd}{Optional child-process working directory.}

\item{timeout}{Positive finite timeout in seconds.}
}
\value{
Invisibly, the normalized configuration path.
}
\description{
Adds one dataset and safely rewrites the YAML file after validating the
complete configuration against the schema and source requirements reported
by the installed datum executable.
}
\examples{
\dontrun{
datum_dataset_add(
  "rates", "Daily exchange rates", "data/rates.csv",
  source = datum_source("http", url = "https://example.test/rates.csv")
)
}
}
