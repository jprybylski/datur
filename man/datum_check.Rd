% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{datum_check}
\alias{datum_check}
\title{Check configured data sources for updates}
\usage{
datum_check(
  targets = NULL,
  config = NULL,
  lock = NULL,
  ...,
  executable = NULL,
  wd = NULL,
  timeout = getOption("datur.timeout", 300),
  concurrency = 1L,
  quiet = getOption("datur.quiet", FALSE)
)
}
\arguments{
\item{targets}{Reserved for target selection. \code{datum} 1.x checks all targets,
so this must currently be \code{NULL}.}

\item{config}{Optional datum configuration path.}

\item{lock}{Optional datum lockfile path.}

\item{...}{Reserved for documented high-level options; currently empty.}

\item{executable}{Optional explicit executable path.}

\item{wd}{Optional child-process working directory.}

\item{timeout}{Positive finite timeout in seconds.}

\item{concurrency}{Number of datasets processed concurrently. Defaults to 1
because \code{datum} documents a shared-target race at higher concurrency.}

\item{quiet}{Suppress automatic interactive printing. Explicit \code{print()}
always works.}
}
\value{
Invisibly, a \code{datur_check_result} object.
}
\description{
Runs \verb{datum --json check}, validates its implicit protocol v1 response, and
returns one normalized record per configured dataset. Changed data is a
successful result even when \code{datum} uses exit status 1 for a \code{fail} policy.
}
\details{
\verb{datum check} always updates its lockfile timestamps and may replace target
files when a dataset uses the \code{update} policy. It does not modify the datum
configuration file.
}
\examples{
if (datum_available()) {
  result <- datum_check(quiet = TRUE)
  result$changed
  as.data.frame(result)
}
}
