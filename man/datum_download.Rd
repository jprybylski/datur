% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{datum_download}
\alias{datum_download}
\title{Download a prebuilt datum executable}
\usage{
datum_download(
  version = "latest",
  destination = NULL,
  overwrite = FALSE,
  timeout = getOption("datur.download_timeout", 60)
)
}
\arguments{
\item{version}{Release version such as \code{"1.3.0"} or \code{"v1.3.0"}. The default,
\code{"latest"}, resolves GitHub's latest published release.}

\item{destination}{Path where the executable should be installed. Defaults to
a package-owned user data directory.}

\item{overwrite}{Replace an existing destination file.}

\item{timeout}{Positive finite GitHub download timeout in seconds.}
}
\value{
A \code{datur_download_result}. When \code{downloaded} is \code{TRUE}, \code{path}
contains the normalized installed executable path. On timeout, \code{url}
contains the manual download link and \code{asset} identifies the correct file.
}
\description{
Downloads the \code{datum} release archive matching the current operating system
and CPU architecture, verifies its published SHA-256 checksum, extracts the
executable, and installs it at \code{destination}. This is an explicit operation;
\code{datur} never downloads or updates executables automatically.
}
\details{
If GitHub times out while resolving or downloading a release, the function
returns a \code{datur_download_result} with \code{downloaded = FALSE} and prints the
appropriate manual release URL and expected asset name.
}
\examples{
\dontrun{
download <- datum_download()
if (download$downloaded) {
  options(datur.datum_path = download$path)
}

datum_download(version = "1.3.0", destination = "~/bin/datum")
}
}
