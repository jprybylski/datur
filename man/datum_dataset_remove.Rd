% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{datum_dataset_remove}
\alias{datum_dataset_remove}
\title{Remove a dataset from \code{.data.yaml}}
\usage{
datum_dataset_remove(
  id,
  delete = NULL,
  config = ".data.yaml",
  lock = NULL,
  executable = NULL,
  wd = NULL,
  timeout = getOption("datur.timeout", 300)
)
}
\arguments{
\item{id}{Unique dataset identifier.}

\item{delete}{Whether to run \code{\link[=datum_delete]{datum_delete()}} before removing the definition.
\code{NULL} asks interactively and otherwise keeps local files.}

\item{config}{Configuration path. A missing file is created with version 1.}

\item{lock}{Optional datum lockfile path used when \code{delete = TRUE}.}

\item{executable}{Optional explicit executable path.}

\item{wd}{Optional child-process working directory.}

\item{timeout}{Positive finite timeout in seconds.}
}
\value{
Invisibly, the normalized configuration path.
}
\description{
Removes a dataset definition after validating the resulting configuration.
In an interactive session, \code{delete = NULL} asks whether its tracked local
files should also be deleted. Deletion runs before the YAML edit so datum can
still resolve the dataset target. In non-interactive use, \code{NULL} is treated
as \code{FALSE}; set \code{delete} explicitly for reproducible scripts.
}
\examples{
\dontrun{
datum_dataset_remove("rates")
datum_dataset_remove("rates", delete = TRUE)
}
}
