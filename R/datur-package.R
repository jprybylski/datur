#' datur: An R interface to datum
#'
#' `datur` runs the external `datum` data update checker without shell
#' interpolation and turns its JSON output into stable R objects.
#'
#' @keywords internal
"_PACKAGE"

