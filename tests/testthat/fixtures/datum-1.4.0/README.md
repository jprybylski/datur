# datum 1.4.0 fixtures

These compact, sanitized documents represent the `schema`, `types`, and
`audit` JSON contracts introduced or available in datum 1.4.0. They contain no
credentials or machine-specific paths. The schema and type fixtures retain the
parts needed to exercise datur's schema-driven configuration editor; the real
release schema is additionally covered by the local integration check used
during development.
