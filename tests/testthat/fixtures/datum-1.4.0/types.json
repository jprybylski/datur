{
  "types": [
    {
      "type": "file",
      "description": "Local file source",
      "fields": [
        {"name": "type", "required": true, "description": "Must be file."},
        {"name": "path", "required": true, "description": "Source path."}
      ]
    },
    {
      "type": "http",
      "description": "HTTP source",
      "fields": [
        {"name": "type", "required": true, "description": "Must be http."},
        {"name": "url", "required": true, "description": "Source URL."}
      ]
    }
  ]
}
